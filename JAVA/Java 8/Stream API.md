#### **Prerequisites
- [[Generics]]
- [[Functional Interface]]
#### **Stream
- Stream is data pipeline process. 
- Without terminal operation it not start to process.
- Stream it used to process or manipulate the datasource without modifying the datasource.
- If the stream runs one time it never allow to run again that stream.
#### **Stream Features
- Lazy evaluation - When terminal operation are invoke to perform the entire stream, for immutability.
- Immutability - When the original dataset are unchanged after perform stream pipeline, for protecting datasource. When stateful operation perform it compute previous result maintaining, so other intermediate operation triggers, it's hard manage pipeline, and also it other intermediate reuse the same streams element it get cause the source.
- It's and functional programming, so it's follows functional programming structures.
- The terminal operation has flag state of the stream. because terminal operation triggers or invoke the streams pipelines.
##### **Three Major part of stream
1. [[#**Several types to create stream object,|Creation part]]
2. [[#**Intermediate Stream|Intermediate Part]]
3. [[#**Terminal Stream|Terminal Part]]
##### **Sub part of stream //TODO
1. Spliterator  
##### **Several types to create stream object

1. Collection.stream() -- creates collection object used to create stream object.
2. Stream.of()  -- creates passing arguments in of() methods creates stream object.
3. Arrays.stream -- creates primitive object to stream object, and it called boxing.
4. Files.readlines / BufferReader.lines -- creates stream object.
5. Pattern.spiltAsStream().
6. Stream.generate() -- creates infinity condition based stream.
7. Stream.iterate() -- creates infinity count based stream.
8. Primitive stream()
##### **Intermediate Stream
- Learn [[Comparable and Comparator]].
##### **Filtering and Truncating Operations

|Operation|Signature|Description|Introduced|
|---|---|---|---|
|**filter**|`Stream<T> filter(Predicate<? super T> predicate)`|Returns elements matching the predicate.|Java 8|
|**takeWhile**|`Stream<T> takeWhile(Predicate<? super T> predicate)`|Takes the longest prefix of elements matching the predicate (ordered streams only).|Java 9|
|**dropWhile**|`Stream<T> dropWhile(Predicate<? super T> predicate)`|Drops the longest prefix of elements matching the predicate (ordered streams only).|Java 9|
|**limit**|`Stream<T> limit(long maxSize)`|Truncates to the first `maxSize` elements.|Java 8|
|**skip**|`Stream<T> skip(long n)`|Discards the first `n` elements.|Java 8|

##### **Mapping Operations

|Operation|Signature|Description|Introduced|
|---|---|---|---|
|**map**|`<R> Stream<R> map(Function<? super T, ? extends R> mapper)`|Transforms each element using the mapper (1:1).|Java 8|
|**mapToInt**|`IntStream mapToInt(ToIntFunction<? super T> mapper)`|Maps to primitive `int` values (avoids boxing).|Java 8|
|**mapToLong**|`LongStream mapToLong(ToLongFunction<? super T> mapper)`|Maps to primitive `long` values.|Java 8|
|**mapToDouble**|`DoubleStream mapToDouble(ToDoubleFunction<? super T> mapper)`|Maps to primitive `double` values.|Java 8|
|**flatMap**|`<R> Stream<R> flatMap(Function<? super T, ? extends Stream<? extends R>> mapper)`|Flattens nested streams (1:many).|Java 8|
|**flatMapToInt**|`IntStream flatMapToInt(Function<? super T, ? extends IntStream> mapper)`|Flattens to primitive `IntStream`.|Java 8|
|**flatMapToLong**|`LongStream flatMapToLong(Function<? super T, ? extends LongStream> mapper)`|Flattens to primitive `LongStream`.|Java 8|
|**flatMapToDouble**|`DoubleStream flatMapToDouble(Function<? super T, ? extends DoubleStream> mapper)`|Flattens to primitive `DoubleStream`.|Java 8|
|**mapMulti**|`<R> Stream<R> mapMulti(BiConsumer<? super T, ? super Consumer<R>> mapper)`|Maps each element to zero or more elements (advanced flattening).|Java 16|
|**mapMultiToInt**|`IntStream mapMultiToInt(BiConsumer<? super T, ? super IntConsumer> mapper)`|Maps to zero or more primitive `int` values.|Java 16|
|**mapMultiToLong**|`LongStream mapMultiToLong(BiConsumer<? super T, ? super LongConsumer> mapper)`|Maps to zero or more primitive `long` values.|Java 16|
|**mapMultiToDouble**|`DoubleStream mapMultiToDouble(BiConsumer<? super T, ? super DoubleConsumer> mapper)`|Maps to zero or more primitive `double` values.|Java 16|

##### **Deduplicating and Sorting Operations

|Operation|Signature|Description|Introduced|
|---|---|---|---|
|**distinct**|`Stream<T> distinct()`|Removes duplicate elements based on `equals()`.|Java 8|
|**sorted**|`Stream<T> sorted()`|Sorts elements in natural order.|Java 8|
|**sorted**|`Stream<T> sorted(Comparator<? super T> comparator)`|Sorts elements using a custom comparator.|Java 8|

##### **Peeking and Gathering Operations

|Operation|Signature|Description|Introduced|
|---|---|---|---|
|**peek**|`Stream<T> peek(Consumer<? super T> action)`|Performs an action (e.g., logging) on each element without modifying it.|Java 8|
|**gather**|`<R> Stream<R> gather(Gatherer<? super T, ?, R> gatherer)`|Applies a custom gatherer for complex transformations (experimental in earlier previews).|Java 24|

##### **Terminal Stream
- When terminal operation only start the execution of stream pipeline, and if terminal operation has involved streams has end.
- Some Terminal operation list,

| Operation        | Signature                                                                                              | Return Type   | Description                                                                  |
| ---------------- | ------------------------------------------------------------------------------------------------------ | ------------- | ---------------------------------------------------------------------------- |
| `forEach`        | `void forEach(Consumer<? super T> action)`                                                             | `void`        | Performs an action for each element (nondeterministic in parallel streams).  |
| `forEachOrdered` | `void forEachOrdered(Consumer<? super T> action)`                                                      | `void`        | Performs an action for each element, respecting encounter order.             |
| `toArray`        | `Object[] toArray()`                                                                                   | `Object[]`    | Returns a runtime Object array of stream elements.                           |
| `toArray`        | `<A> A[] toArray(IntFunction<A[]> generator)`                                                          | `A[]`         | Returns an array using a provided generator function.                        |
| `reduce`         | `Optional<T> reduce(BinaryOperator<T> accumulator)`                                                    | `Optional<T>` | Reduces elements to a single value using an accumulator (associative).       |
| `reduce`         | `T reduce(T identity, BinaryOperator<T> accumulator)`                                                  | `T`           | Reduces with an identity value and accumulator.                              |
| `reduce`         | `<U> U reduce(U identity, BiFunction<U, ? super T, U> accumulator, BinaryOperator<U> combiner)`        | `U`           | Reduces with identity, accumulator, and combiner (for parallel).             |
| `collect`        | `<R, A> R collect(Collector<? super T, A, R> collector)`                                               | `R`           | Accumulates into a result using a Collector (e.g., `toList()`, `joining()`). |
| `collect`        | `<R> R collect(Supplier<R> supplier, BiConsumer<R, ? super T> accumulator, BiConsumer<R, R> combiner)` | `R`           | Mutable reduction with explicit supplier, accumulator, and combiner.         |
| `toList`         | `default List<T> toList()`                                                                             | `List<T>`     | Collects elements into an unmodifiable `List` (Java 16+).                    |
| `min`            | `Optional<T> min(Comparator<? super T> comparator)`                                                    | `Optional<T>` | Returns the minimum element per comparator (empty if stream empty).          |
| `max`            | `Optional<T> max(Comparator<? super T> comparator)`                                                    | `Optional<T>` | Returns the maximum element per comparator (empty if stream empty).          |
| `count`          | `long count()`                                                                                         | `long`        | Returns the number of elements in the stream.                                |
| `anyMatch`*      | `boolean anyMatch(Predicate<? super T> predicate)`                                                     | `boolean`     | Returns `true` if any element matches the predicate.                         |
| `allMatch`*      | `boolean allMatch(Predicate<? super T> predicate)`                                                     | `boolean`     | Returns `true` if all elements match (or stream empty).                      |
| `noneMatch`*     | `boolean noneMatch(Predicate<? super T> predicate)`                                                    | `boolean`     | Returns `true` if no elements match (or stream empty).                       |
| `findFirst`*     | `Optional<T> findFirst()`                                                                              | `Optional<T>` | Returns the first element (respects order).                                  |
| `findAny`*       | `Optional<T> findAny()`                                                                                | `Optional<T>` | Returns any element (nondeterministic, good for parallel).                   |
#### **Coll Collectors Clasector Interface &s
- Collectors class has implemented in Collectors Interface, to collects processed data.
- Collector interface has [[Functional Interface|Predefined Functional interface.]]
- Collector interface has .of() method to custom collectors function initialization.

**Collector class methods 

| Method Name              | Signature                                                                                                                                                                                                                                | Explanation                                                                                                                                                                              | Parameters                                                                                                  | Return Type & Details                                                             | Example                                                                                                                                                                                                                                                  |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| averagingDouble          | `static <T> Collector<T, ?, Double> averagingDouble(ToDoubleFunction<? super T> mapper)`                                                                                                                                                 | Computes the arithmetic mean (average) of values extracted from input elements via a double-producing function. If no elements, returns 0.0. Handles overflow by using double precision. | `mapper`: Function to extract a double value from each element (e.g., a property getter).                   | `Collector<T, ?, Double>`: The average as a `Double`.                             | ```java:disable-run                                                                                                                                                                                                                                      |
| averagingInt             | `static <T> Collector<T, ?, Double> averagingInt(ToIntFunction<? super T> mapper)`                                                                                                                                                       | Computes the arithmetic mean of integer values extracted from elements. Returns 0.0 if empty. Uses internal summation to avoid overflow, then divides.                                   | `mapper`: Function extracting an int from each element.                                                     | `Collector<T, ?, Double>`: Average as `Double`.                                   | `java<br>List<Integer> numbers = List.of(1, 2, 3, 4);<br>double avg = numbers.stream().collect(Collectors.averagingInt(Integer::intValue));<br>// avg = 2.5<br>`                                                                                         |
| averagingLong            | `static <T> Collector<T, ?, Double> averagingLong(ToLongFunction<? super T> mapper)`                                                                                                                                                     | Computes the arithmetic mean of long values extracted from elements. Returns 0.0 if empty. Similar to averagingInt but for longs.                                                        | `mapper`: Function extracting a long from each element.                                                     | `Collector<T, ?, Double>`: Average as `Double`.                                   | `java<br>List<Long> numbers = List.of(1L, 2L, 3L, 4L);<br>double avg = numbers.stream().collect(Collectors.averagingLong(Long::longValue));<br>// avg = 2.5<br>`                                                                                         |
| collectingAndThen        | `static <T, A, R, RR> Collector<T, A, RR> collectingAndThen(Collector<T, A, R> downstream, Function<R, RR> finisher)`                                                                                                                    | Wraps another collector and applies a final transformation to its result after accumulation completes. Useful for post-processing, like making collections immutable.                    | `downstream`: The base collector to accumulate into. `finisher`: Function applied to the downstream result. | `Collector<T, A, RR>`: A collector that performs the extra finishing step.        | `java<br>List<String> names = List.of("Alice", "Bob");<br>List<String> immutable = names.stream().collect(Collectors.collectingAndThen(Collectors.toList(), Collections::unmodifiableList));<br>// immutable is an unmodifiable view<br>`                |
| counting                 | `static <T> Collector<T, ?, Long> counting()`                                                                                                                                                                                            | Simply counts the number of input elements in the stream. Efficient and parallel-friendly. Returns 0 for empty streams.                                                                  | None.                                                                                                       | `Collector<T, ?, Long>`: The count as a `Long`.                                   | `java<br>List<String> items = List.of("a", "b", "c");<br>long count = items.stream().collect(Collectors.counting());<br>// count = 3<br>`                                                                                                                |
| filtering                | `static <T, A, R> Collector<T, ?, R> filtering(Predicate<? super T> predicate, Collector<? super T, A, R> downstream)`                                                                                                                   | Filters elements using a predicate before passing them to a downstream collector. Only matching elements are accumulated. (Introduced in Java 9.)                                        | `predicate`: Test to filter elements. `downstream`: Collector for filtered elements.                        | `Collector<T, ?, R>`: Result of the downstream collector on filtered elements.    | `java<br>Map<Integer, List<Integer>> evensByMod = List.of(1,2,3,4,5,6).stream()<br> .collect(Collectors.groupingBy(i -> i % 3,<br> Collectors.filtering(i -> i % 2 == 0, Collectors.toList())));<br>// {0=[6], 1=[2,4], 2=[]}<br>`                       |
| flatMapping              | `static <T, U, A, R> Collector<T, ?, R> flatMapping(Function<? super T, ? extends Stream<? extends U>> mapper, Collector<? super U, A, R> downstream)`                                                                                   | Applies a function that maps each element to a Stream, flattens those streams, and accumulates the results into a downstream collector. (Introduced in Java 9.)                          | `mapper`: Function returning a Stream for each element. `downstream`: Collector for flattened elements.     | `Collector<T, ?, R>`: Result of downstream on flattened values.                   | `java<br>List<List<String>> nested = List.of(List.of("a","b"), List.of("c"));<br>Set<String> flatSet = nested.stream().collect(Collectors.flatMapping(List::stream, Collectors.toSet()));<br>// {"a","b","c"}<br>`                                       |
| groupingBy (1)           | `static <T, K> Collector<T, ?, Map<K, List<T>>> groupingBy(Function<? super T, ? extends K> classifier)`                                                                                                                                 | Groups elements into a Map where keys are from the classifier function, and values are Lists of elements sharing the key. Uses HashMap internally.                                       | `classifier`: Function to compute the key for each element.                                                 | `Collector<T, ?, Map<K, List<T>>>`: Map of keys to lists of elements.             | `java<br>Map<Integer, List<Integer>> byMod = List.of(1,2,3,4).stream()<br> .collect(Collectors.groupingBy(i -> i % 2));<br>// {0=[2,4], 1=[1,3]}<br>`                                                                                                    |
| groupingBy (2)           | `static <T, K, A, D> Collector<T, ?, Map<K, D>> groupingBy(Function<? super T, ? extends K> classifier, Collector<? super T, A, D> downstream)`                                                                                          | Groups elements by key, then applies a downstream collector to each group (e.g., count, sum, or set).                                                                                    | `classifier`: Key function. `downstream`: Reduction for each group.                                         | `Collector<T, ?, Map<K, D>>`: Map of keys to reduced group values.                | `java<br>Map<Integer, Long> countByMod = List.of(1,2,3,4).stream()<br> .collect(Collectors.groupingBy(i -> i % 2, Collectors.counting()));<br>// {0=2, 1=2}<br>`                                                                                         |
| groupingBy (3)           | `static <T, K, D, A, M extends Map<K, D>> Collector<T, ?, M> groupingBy(Function<? super T, ? extends K> classifier, Supplier<M> mapFactory, Collector<? super T, A, D> downstream)`                                                     | Like above, but allows supplying a custom Map implementation (e.g., TreeMap for sorted keys).                                                                                            | `classifier`, `mapFactory`: Supplier for the Map. `downstream`.                                             | `Collector<T, ?, M>`: Custom Map of keys to reduced values.                       | `java<br>TreeMap<Integer, Set<Integer>> sortedByMod = List.of(1,2,3,4).stream()<br> .collect(Collectors.groupingBy(i -> i % 2, TreeMap::new, Collectors.toSet()));<br>// TreeMap with keys 0 and 1<br>`                                                  |
| groupingByConcurrent (1) | `static <T, K> Collector<T, ?, ConcurrentMap<K, List<T>>> groupingByConcurrent(Function<? super T, ? extends K> classifier)`                                                                                                             | Concurrent version of groupingBy, safe for parallel streams, producing a ConcurrentHashMap.                                                                                              | `classifier`: Key function.                                                                                 | `Collector<T, ?, ConcurrentMap<K, List<T>>>`: Concurrent map of keys to lists.    | Similar to groupingBy (1), but use `.parallelStream()` for concurrency benefits.                                                                                                                                                                         |
| groupingByConcurrent (2) | `static <T, K, A, D> Collector<T, ?, ConcurrentMap<K, D>> groupingByConcurrent(Function<? super T, ? extends K> classifier, Collector<? super T, A, D> downstream)`                                                                      | Concurrent grouping with downstream reduction per group.                                                                                                                                 | `classifier`, `downstream`.                                                                                 | `Collector<T, ?, ConcurrentMap<K, D>>`: Concurrent map of keys to reduced values. | Similar to groupingBy (2), for parallel streams.                                                                                                                                                                                                         |
| groupingByConcurrent (3) | `static <T, K, A, D, M extends ConcurrentMap<K, D>> Collector<T, ?, M> groupingByConcurrent(Function<? super T, ? extends K> classifier, Supplier<M> mapFactory, Collector<? super T, A, D> downstream)`                                 | Concurrent grouping with custom concurrent map factory.                                                                                                                                  | `classifier`, `mapFactory`, `downstream`.                                                                   | `Collector<T, ?, M>`: Custom concurrent map.                                      | Similar to groupingBy (3), e.g., with `ConcurrentSkipListMap::new` for sorted concurrent map.                                                                                                                                                            |
| joining (1)              | `static Collector<CharSequence, ?, String> joining()`                                                                                                                                                                                    | Concatenates CharSequence elements (e.g., Strings) into a single String with no delimiter.                                                                                               | None.                                                                                                       | `Collector<CharSequence, ?, String>`: Concatenated string.                        | `java<br>String result = List.of("a", "b", "c").stream().collect(Collectors.joining());<br>// "abc"<br>`                                                                                                                                                 |
| joining (2)              | `static Collector<CharSequence, ?, String> joining(CharSequence delimiter)`                                                                                                                                                              | Concatenates with a specified delimiter between elements.                                                                                                                                | `delimiter`: String separator.                                                                              | `Collector<CharSequence, ?, String>`: Delimited string.                           | `java<br>String result = List.of("a", "b", "c").stream().collect(Collectors.joining(", "));<br>// "a, b, c"<br>`                                                                                                                                         |
| joining (3)              | `static Collector<CharSequence, ?, String> joining(CharSequence delimiter, CharSequence prefix, CharSequence suffix)`                                                                                                                    | Concatenates with delimiter, adding prefix and suffix to the whole result.                                                                                                               | `delimiter`, `prefix`, `suffix`.                                                                            | `Collector<CharSequence, ?, String>`: Formatted string.                           | `java<br>String result = List.of("a", "b").stream().collect(Collectors.joining(", ", "[", "]"));<br>// "[a, b]"<br>`                                                                                                                                     |
| mapping                  | `static <T, U, A, R> Collector<T, ?, R> mapping(Function<? super T, ? extends U> mapper, Collector<? super U, A, R> downstream)`                                                                                                         | Maps each element to a new value/type before accumulating into the downstream collector. Often used in grouping/partitioning.                                                            | `mapper`: Transformation function. `downstream`: Collector for mapped values.                               | `Collector<T, ?, R>`: Result of downstream on mapped elements.                    | `java<br>Map<Integer, Set<String>> stringsByLength = List.of("one", "two", "three").stream()<br> .collect(Collectors.groupingBy(String::length, Collectors.mapping(String::toUpperCase, Collectors.toSet())));<br>// {3={"ONE","TWO"}, 5={"THREE"}}<br>` |
| maxBy                    | `static <T> Collector<T, ?, Optional<T>> maxBy(Comparator<? super T> comparator)`                                                                                                                                                        | Finds the maximum element based on a comparator. Returns Optional.empty() if no elements.                                                                                                | `comparator`: For comparing elements.                                                                       | `Collector<T, ?, Optional<T>>`: Optional containing the max element.              | `java<br>Optional<Integer> max = List.of(1,3,2).stream().collect(Collectors.maxBy(Integer::compareTo));<br>// Optional[3]<br>`                                                                                                                           |
| minBy                    | `static <T> Collector<T, ?, Optional<T>> minBy(Comparator<? super T> comparator)`                                                                                                                                                        | Finds the minimum element based on a comparator. Returns Optional.empty() if empty.                                                                                                      | `comparator`: For comparing elements.                                                                       | `Collector<T, ?, Optional<T>>`: Optional containing the min element.              | `java<br>Optional<Integer> min = List.of(1,3,2).stream().collect(Collectors.minBy(Integer::compareTo));<br>// Optional[1]<br>`                                                                                                                           |
| partitioningBy (1)       | `static <T> Collector<T, ?, Map<Boolean, List<T>>> partitioningBy(Predicate<? super T> predicate)`                                                                                                                                       | Partitions elements into a Map with Boolean keys: true for matching the predicate, false otherwise. Values are Lists.                                                                    | `predicate`: Classification test.                                                                           | `Collector<T, ?, Map<Boolean, List<T>>>`: Map with true/false keys to lists.      | `java<br>Map<Boolean, List<Integer>> evenOdd = List.of(1,2,3,4).stream()<br> .collect(Collectors.partitioningBy(i -> i % 2 == 0));<br>// {false=[1,3], true=[2,4]}<br>`                                                                                  |
| partitioningBy (2)       | `static <T, D, A> Collector<T, ?, Map<Boolean, D>> partitioningBy(Predicate<? super T> predicate, Collector<? super T, A, D> downstream)`                                                                                                | Partitions and applies downstream collector to each partition.                                                                                                                           | `predicate`, `downstream`.                                                                                  | `Collector<T, ?, Map<Boolean, D>>`: Map with true/false to reduced values.        | `java<br>Map<Boolean, Long> countEvenOdd = List.of(1,2,3,4).stream()<br> .collect(Collectors.partitioningBy(i -> i % 2 == 0, Collectors.counting()));<br>// {false=2, true=2}<br>`                                                                       |
| reducing (1)             | `static <T> Collector<T, ?, T> reducing(T identity, BinaryOperator<T> op)`                                                                                                                                                               | Reduces elements starting from an identity value using a binary operator (associative).                                                                                                  | `identity`: Starting value. `op`: Reduction operator.                                                       | `Collector<T, ?, T>`: Reduced value (identity if empty).                          | `java<br>int sum = List.of(1,2,3).stream().collect(Collectors.reducing(0, Integer::sum));<br>// 6<br>`                                                                                                                                                   |
| reducing (2)             | `static <T> Collector<T, ?, Optional<T>> reducing(BinaryOperator<T> op)`                                                                                                                                                                 | Reduces without identity; returns Optional.empty() if empty. First element is initial if present.                                                                                        | `op`: Reduction operator.                                                                                   | `Collector<T, ?, Optional<T>>`: Optional reduced value.                           | `java<br>Optional<Integer> max = List.of(1,3,2).stream().collect(Collectors.reducing(Integer::max));<br>// Optional[3]<br>`                                                                                                                              |
| reducing (3)             | `static <T, U> Collector<T, ?, U> reducing(U identity, Function<? super T, ? extends U> mapper, BinaryOperator<U> op)`                                                                                                                   | Maps elements first, then reduces the mapped values with identity and operator.                                                                                                          | `identity`, `mapper`, `op`.                                                                                 | `Collector<T, ?, U>`: Reduced mapped value.                                       | `java<br>String longest = List.of("apple", "banana", "cherry").stream()<br> .collect(Collectors.reducing("", String::toString, (a,b) -> a.length() > b.length() ? a : b));<br>// "banana"<br>`                                                           |
| summarizingDouble        | `static <T> Collector<T, ?, DoubleSummaryStatistics> summarizingDouble(ToDoubleFunction<? super T> mapper)`                                                                                                                              | Computes statistics (count, sum, min, max, average) on double values extracted from elements.                                                                                            | `mapper`: Double extractor.                                                                                 | `Collector<T, ?, DoubleSummaryStatistics>`: Statistics object.                    | `java<br>DoubleSummaryStatistics stats = List.of(1.0, 2.5, 3.0).stream().collect(Collectors.summarizingDouble(Double::doubleValue));<br>// count=3, sum=6.5, min=1.0, average=2.166, max=3.0<br>`                                                        |
| summarizingInt           | `static <T> Collector<T, ?, IntSummaryStatistics> summarizingInt(ToIntFunction<? super T> mapper)`                                                                                                                                       | Similar to summarizingDouble but for int values.                                                                                                                                         | `mapper`: Int extractor.                                                                                    | `Collector<T, ?, IntSummaryStatistics>`: Int statistics.                          | Similar to summarizingDouble, but with ints.                                                                                                                                                                                                             |
| summarizingLong          | `static <T> Collector<T, ?, LongSummaryStatistics> summarizingLong(ToLongFunction<? super T> mapper)`                                                                                                                                    | Similar for long values.                                                                                                                                                                 | `mapper`: Long extractor.                                                                                   | `Collector<T, ?, LongSummaryStatistics>`: Long statistics.                        | Similar to summarizingDouble, but with longs.                                                                                                                                                                                                            |
| summingDouble            | `static <T> Collector<T, ?, Double> summingDouble(ToDoubleFunction<? super T> mapper)`                                                                                                                                                   | Sums double values extracted from elements. Returns 0.0 if empty.                                                                                                                        | `mapper`: Double extractor.                                                                                 | `Collector<T, ?, Double>`: Sum as Double.                                         | `java<br>double sum = List.of(1.0, 2.5).stream().collect(Collectors.summingDouble(Double::doubleValue));<br>// 3.5<br>`                                                                                                                                  |
| summingInt               | `static <T> Collector<T, ?, Integer> summingInt(ToIntFunction<? super T> mapper)`                                                                                                                                                        | Sums int values. Returns 0 if empty.                                                                                                                                                     | `mapper`: Int extractor.                                                                                    | `Collector<T, ?, Integer>`: Sum as Integer.                                       | `java<br>int sum = List.of(1,2,3).stream().collect(Collectors.summingInt(Integer::intValue));<br>// 6<br>`                                                                                                                                               |
| summingLong              | `static <T> Collector<T, ?, Long> summingLong(ToLongFunction<? super T> mapper)`                                                                                                                                                         | Sums long values. Returns 0 if empty.                                                                                                                                                    | `mapper`: Long extractor.                                                                                   | `Collector<T, ?, Long>`: Sum as Long.                                             | Similar to summingInt, but with longs.                                                                                                                                                                                                                   |
| teeing                   | `static <T, R1, R2, R> Collector<T, ?, R> teeing(Collector<? super T, ?, R1> downstream1, Collector<? super T, ?, R2> downstream2, BiFunction<? super R1, ? super R2, R> merger)`                                                        | "Tees" the stream to two downstream collectors, then merges their results. (Introduced in Java 12.)                                                                                      | `downstream1`, `downstream2`: Two collectors. `merger`: Combines their results.                             | `Collector<T, ?, R>`: Merged result.                                              | `java<br>double[] minMax = List.of(1,3,2).stream().collect(Collectors.teeing(<br> Collectors.minBy(Integer::compareTo),<br> Collectors.maxBy(Integer::compareTo),<br> (min, max) -> new double[]{min.orElse(0), max.orElse(0)}));<br>// [1.0, 3.0]<br>`  |
| toCollection             | `static <T, C extends Collection<T>> Collector<T, ?, C> toCollection(Supplier<C> collectionFactory)`                                                                                                                                     | Accumulates into a custom Collection type provided by the factory.                                                                                                                       | `collectionFactory`: Supplier for the Collection (e.g., ArrayList::new).                                    | `Collector<T, ?, C>`: The populated Collection.                                   | `java<br>LinkedList<Integer> list = List.of(1,2,3).stream().collect(Collectors.toCollection(LinkedList::new));<br>`                                                                                                                                      |
| toConcurrentMap (1)      | `static <T, K, U> Collector<T, ?, ConcurrentMap<K, U>> toConcurrentMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper)`                                                                        | Accumulates into a ConcurrentHashMap with mapped keys/values. Throws on duplicate keys.                                                                                                  | `keyMapper`, `valueMapper`.                                                                                 | `Collector<T, ?, ConcurrentMap<K, U>>`: Concurrent map.                           | Similar to toMap, but concurrent.                                                                                                                                                                                                                        |
| toConcurrentMap (2)      | `static <T, K, U> Collector<T, ?, ConcurrentMap<K, U>> toConcurrentMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper, BinaryOperator<U> mergeFunction)`                                       | With merge function for duplicate keys.                                                                                                                                                  | `keyMapper`, `valueMapper`, `mergeFunction`.                                                                | `Collector<T, ?, ConcurrentMap<K, U>>`: Concurrent map.                           | `java<br>ConcurrentMap<String, Integer> map = List.of("a:1", "a:2").stream().collect(Collectors.toConcurrentMap(<br> s -> s.split(":")[0], s -> Integer.parseInt(s.split(":")[1]), Integer::sum));<br>// {"a"=3}<br>`                                    |
| toConcurrentMap (3)      | `static <T, K, U, M extends ConcurrentMap<K, U>> Collector<T, ?, M> toConcurrentMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper, BinaryOperator<U> mergeFunction, Supplier<M> mapSupplier)` | With custom concurrent map supplier.                                                                                                                                                     | `keyMapper`, `valueMapper`, `mergeFunction`, `mapSupplier`.                                                 | `Collector<T, ?, M>`: Custom concurrent map.                                      | Similar, e.g., with `ConcurrentSkipListMap::new`.                                                                                                                                                                                                        |
| toList                   | `static <T> Collector<T, ?, List<T>> toList()`                                                                                                                                                                                           | Accumulates into an ArrayList in encounter order.                                                                                                                                        | None.                                                                                                       | `Collector<T, ?, List<T>>`: Populated List.                                       | `java<br>List<Integer> list = Stream.of(1,2,3).collect(Collectors.toList());<br>`                                                                                                                                                                        |
| toMap (1)                | `static <T, K, U> Collector<T, ?, Map<K, U>> toMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper)`                                                                                            | Accumulates into a HashMap with mapped keys/values. Throws IllegalStateException on duplicate keys.                                                                                      | `keyMapper`, `valueMapper`.                                                                                 | `Collector<T, ?, Map<K, U>>`: Populated Map.                                      | `java<br>Map<Integer, String> map = List.of(1,2).stream().collect(Collectors.toMap(i -> i, i -> "val"+i));<br>// {1="val1", 2="val2"}<br>`                                                                                                               |
| toMap (2)                | `static <T, K, U> Collector<T, ?, Map<K, U>> toMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper, BinaryOperator<U> mergeFunction)`                                                           | With merge function to resolve duplicate keys (e.g., sum values).                                                                                                                        | `keyMapper`, `valueMapper`, `mergeFunction`.                                                                | `Collector<T, ?, Map<K, U>>`: Map with merged duplicates.                         | `java<br>Map<String, Integer> map = List.of("a:1", "a:2").stream().collect(Collectors.toMap(<br> s -> s.split(":")[0], s -> Integer.parseInt(s.split(":")[1]), Integer::sum));<br>// {"a"=3}<br>`                                                        |
| toMap (3)                | `static <T, K, U, M extends Map<K, U>> Collector<T, ?, M> toMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper, BinaryOperator<U> mergeFunction, Supplier<M> mapSupplier)`                     | With custom Map supplier (e.g., TreeMap).                                                                                                                                                | `keyMapper`, `valueMapper`, `mergeFunction`, `mapSupplier`.                                                 | `Collector<T, ?, M>`: Custom Map.                                                 | Similar to toMap (2), but with `LinkedHashMap::new` for insertion order.                                                                                                                                                                                 |
| toSet                    | `static <T> Collector<T, ?, Set<T>> toSet()`                                                                                                                                                                                             | Accumulates into a HashSet (no order guarantees, no duplicates).                                                                                                                         | None.                                                                                                       | `Collector<T, ?, Set<T>>`: Populated Set.                                         | `java<br>Set<Integer> set = Stream.of(1,2,2).collect(Collectors.toSet());<br>// {1,2}<br>`                                                                                                                                                               |
| toUnmodifiableList       | `static <T> Collector<T, ?, List<T>> toUnmodifiableList()`                                                                                                                                                                               | Accumulates into an unmodifiable List (throws on modification attempts). (Java 10+.)                                                                                                     | None.                                                                                                       | `Collector<T, ?, List<T>>`: Unmodifiable List.                                    | Similar to toList, but result is immutable.                                                                                                                                                                                                              |
| toUnmodifiableMap (1)    | `static <T, K, U> Collector<T, ?, Map<K, U>> toUnmodifiableMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper)`                                                                                | Accumulates into an unmodifiable Map. Throws on duplicates. (Java 10+.)                                                                                                                  | `keyMapper`, `valueMapper`.                                                                                 | `Collector<T, ?, Map<K, U>>`: Unmodifiable Map.                                   | Similar to toMap (1), but immutable.                                                                                                                                                                                                                     |
| toUnmodifiableMap (2)    | `static <T, K, U> Collector<T, ?, Map<K, U>> toUnmodifiableMap(Function<? super T, ? extends K> keyMapper, Function<? super T, ? extends U> valueMapper, BinaryOperator<U> mergeFunction)`                                               | With merge for duplicates, result unmodifiable.                                                                                                                                          | `keyMapper`, `valueMapper`, `mergeFunction`.                                                                | `Collector<T, ?, Map<K, U>>`: Unmodifiable Map.                                   | Similar to toMap (2), but immutable.                                                                                                                                                                                                                     |
| toUnmodifiableSet        | `static <T> Collector<T, ?, Set<T>> toUnmodifiableSet()`                                                                                                                                                                                 | Accumulates into an unmodifiable Set. (Java 10+.)                                                                                                                                        | None.                                                                                                       | `Collector<T, ?, Set<T>>`: Unmodifiable Set.                                      | Similar to toSet, but immutable.                                                                                                                                                                                                                         |