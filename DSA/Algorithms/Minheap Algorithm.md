#### **Purpose
- Minheap using Binary tree, and the root element must be minimum by default.
- It takes insertion / deletion in O(log n). Overall processing time O(n log n).
- It's not fully sort tree instead it's using partial sort array.
- Internally uses linked list.

#### **Working Principle
- **Insertion :**
	1. Insertion using Bubble up technics.
	2. Deletion / Remove using Bubble Down technics.
	3. 